package com.edifika.iam.authentication.domain.model.valueobjects;

/**
 * Enum que define los roles disponibles en el sistema Edifika.
 * Por ahora solo se maneja el rol de administrador.
 */
public enum Roles {
    ADMIN,
    OWNER,
    TENANT
}
